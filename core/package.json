{
	"name": "modulist-core",
	"version": "0.0.1",
	"description": "Modulist core",
	"main": "index.js",
	"scripts": {
		"tsc": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "ts-node-dev --respawn --transpileOnly ./src/main.ts",
		"prod": "tsc && node ./build/main.js"
	},
	"author": "Teemu Karppinen",
	"license": "MIT",
	"dependencies": {
		"ts-node-dev": "^1.0.0-pre.39",
		"typescript": "^3.4.5"
	},
	"devDependencies": {
		"@types/node": "^12.0.2"
	}
}
